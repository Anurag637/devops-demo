apiVersion: v1
kind: Service
metadata:
  name: docker-lab-service
spec:
  type: NodePort
  selector:
    app: docker-lab
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
